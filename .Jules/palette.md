## 2026-01-23 - [Dual Rendering Contexts]
**Learning:** Admin interfaces use both PHP loops (initial render) and JS string concatenation (dynamic rows). Accessibility attributes must be manually synchronized across both contexts.
**Action:** When fixing admin table accessibility, search for corresponding JS templates to ensure dynamic rows match the PHP-rendered ones.
