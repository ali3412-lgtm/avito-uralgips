## 2024-10-24 - Admin UI Construction Pattern
**Learning:** Admin interfaces (like field settings) are constructed via manual HTML concatenation in PHP and JS strings, with CSS embedded in the same file.
**Action:** When modifying UI, check both PHP loops (initial render) and JS template strings (dynamic rows). Match deep indentation in JS strings carefully.
