## 2026-01-23 - Duplicate Accessibility in Hybrid PHP/JS\n**Learning:** When fixing accessibility in PHP files that also use inline JS to append rows, you must duplicate the attributes (like `aria-label`) in the JS string concatenation to ensure consistency for new elements.\n**Action:** Always search for the HTML string in the JS section of the same file when modifying the PHP render loop.
