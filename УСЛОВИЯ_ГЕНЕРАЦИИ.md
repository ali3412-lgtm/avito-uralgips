# Условия генерации полей - Руководство пользователя

## Что это такое?

Условия генерации полей позволяют вам указывать, когда именно определенное поле должно быть добавлено в XML для Avito. Это полезно, когда вы хотите, чтобы поля появлялись только для определенных категорий товаров, ценовых диапазонов или других условий.

## Как это работает?

1. Перейдите в **WooCommerce → Поля XML**
2. В таблице **"Общие поля (для всех объявлений)"** найдите колонку **"Условие генерации"**
3. Введите условие в специальном формате
4. Поле будет добавлено в XML только если условие выполнено

## Формат условия

```
{плейсхолдер} оператор значение
```

### Доступные плейсхолдеры

Вы можете использовать все те же плейсхолдеры, что и в значениях полей:

#### Стандартные данные товара:
- `{product_id}` - ID товара
- `{product_name}` - Название товара
- `{product_sku}` - Артикул
- `{product_price}` - Цена
- `{product_regular_price}` - Обычная цена
- `{product_sale_price}` - Цена со скидкой
- `{product_stock}` - Количество на остатке
- `{category_name}` - Название категории
- `{current_date}` - Текущая дата

#### Произвольные поля:
- `{meta:имя_поля}` - Произвольное поле товара
- `{term_meta:имя_поля}` - Произвольное поле категории
- `{attribute:имя_атрибута}` - Атрибут товара

### Доступные операторы

| Оператор | Описание | Пример |
|----------|----------|--------|
| `=` | Равно | `{category_name}=Керамзит` |
| `!=` | Не равно | `{category_name}!=Песок` |
| `>` | Больше | `{product_price}>1000` |
| `<` | Меньше | `{product_price}<5000` |
| `>=` | Больше или равно | `{product_price}>=1500` |
| `<=` | Меньше или равно | `{product_price}<=3000` |
| `contains` | Содержит (без учета регистра) | `{product_name} contains кирпич` |
| `!contains` | Не содержит (без учета регистра) | `{product_name} !contains песок` |

## Практические примеры

### Пример 1: Поле только для определенной категории

**Задача:** Добавить поле "SpecialOffer" только для товаров из категории "Керамзит"

```
Условие: {category_name}=Керамзит
```

### Пример 2: Поле для дорогих товаров

**Задача:** Добавить поле "PremiumService" только для товаров дороже 5000 рублей

```
Условие: {product_price}>5000
```

### Пример 3: Поле для товаров с определенным словом в названии

**Задача:** Добавить специальное поле для всех кирпичей

```
Условие: {product_name} contains кирпич
```

### Пример 4: Поле для товаров в наличии

**Задача:** Добавить поле "FastDelivery" только для товаров в наличии

```
Условие: {meta:_stock_status}=instock
```

### Пример 5: Поле для товаров с определенным атрибутом

**Задача:** Добавить поле только для товаров красного цвета

```
Условие: {attribute:pa_color}=красный
```

### Пример 6: Комбинация с ценовым диапазоном

**Задача:** Добавить поле для товаров средней ценовой категории (от 1000 до 5000 рублей)

Вариант 1 (добавляем два поля с разными условиями):
```
Поле 1 условие: {product_price}>=1000
Поле 2 условие: {product_price}<=5000
```

*Примечание: в будущих версиях может быть добавлена поддержка логических операторов AND/OR*

### Пример 7: Исключение категории

**Задача:** Добавить поле для всех товаров, кроме категории "Распродажа"

```
Условие: {category_name}!=Распродажа
```

## Пошаговая инструкция

### Шаг 1: Создайте или откройте поле

1. Откройте **WooCommerce → Поля XML**
2. Нажмите **"Добавить поле"** или откройте существующее глобальное поле

### Шаг 2: Заполните название и значение

1. Укажите название XML тега (например, `SpecialCategory`)
2. Введите значение поля (может содержать плейсхолдеры)
3. Выберите тип поля

### Шаг 3: Добавьте условие

В поле "Условие генерации" введите условие, например:
```
{category_name}=Керамзит
```

### Шаг 4: Сохраните настройки

Нажмите **"Сохранить настройки полей"**

### Шаг 5: Проверьте результат

1. Перейдите в **WooCommerce → Avito Export**
2. Нажмите **"Сгенерировать XML"**
3. Скачайте файл и проверьте, что поле появляется только у нужных товаров

## Отладка

### Поле не появляется в XML

Проверьте:
1. ✅ Поле включено (галочка в колонке "Вкл.")
2. ✅ Условие написано правильно (проверьте синтаксис)
3. ✅ Плейсхолдеры в условии корректны
4. ✅ У товара есть данные для проверки (категория заполнена, цена установлена и т.д.)

### Поле появляется у всех товаров

- Если условие не указано или пустое, поле генерируется для всех товаров
- Проверьте, что условие действительно сохранилось

### Проверка логов

При неверном формате условия ошибка записывается в лог WordPress. Включите отладку:

```php
// В wp-config.php
define('WP_DEBUG', true);
define('WP_DEBUG_LOG', true);
```

Логи будут в файле `wp-content/debug.log`

## Важные замечания

⚠️ **Внимание:**
- Условия работают только для **глобальных полей**
- Для полей категорий и товаров используйте стандартные механизмы (заполнение/незаполнение значений)
- Условия проверяются для каждого товара при генерации XML
- Регистр букв в операторе `contains` не важен (поиск регистронезависимый)

## Технические детали

### Порядок обработки

1. Плейсхолдеры в условии заменяются на реальные значения товара
2. Проверяется выполнение условия
3. Если условие выполнено:
   - Обрабатываются плейсхолдеры в значении поля
   - Поле добавляется в XML
4. Если условие не выполнено - поле пропускается

### Пустые условия

Если поле "Условие генерации" оставить пустым, поле будет генерироваться для всех товаров (поведение по умолчанию).

## Примеры из практики

### Кейс 1: Разные адреса для разных категорий

```
Название тега: Address
Значение: г. Ижевск, ул. Складская, 10
Условие: {category_name}=Кирпич

Название тега: Address
Значение: г. Ижевск, ул. Промышленная, 5
Условие: {category_name}=Керамзит
```

### Кейс 2: Специальное описание для акционных товаров

```
Название тега: SpecialOffer
Значение: 🔥 Акция! Скидка {product_sale_price} руб!
Условие: {product_sale_price}>0
```

### Кейс 3: Пометка товаров на остатке

```
Название тега: InStockBadge
Значение: ✓ В наличии
Условие: {product_stock}>0
```

## Поддержка

Если у вас возникли вопросы или проблемы с использованием условий генерации:

1. Проверьте примеры в этом документе
2. Убедитесь, что синтаксис условия правильный
3. Проверьте логи WordPress на наличие ошибок
4. Обратитесь к разработчику для дополнительной помощи
